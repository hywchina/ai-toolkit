# LoRA 服务容器化实施计划

目标：当前 AI Toolkit，单镜像包含 Next.js API、队列 Worker 和 Python 训练环境。模型通过只读挂载提供；数据库、数据集和输出持久化。所有 API 测试按顺序执行。

## 工作顺序

1. 检查并安装导出的 Python 3.12 环境，启动本地服务。
2. 增加可重复执行的串行 API 冒烟测试；使用唯一测试任务和数据集，避免修改业务任务。测试鉴权、数据集上传、任务 CRUD、队列、日志、指标、产物以及启动错误处理。
3. 修复测试暴露的问题，按功能提交。训练能力另用小型模型验证，报告须区分 API 可用性与真实训练结果。
4. 编写独立服务 Dockerfile、构建上下文排除规则、启动脚本和 Compose 配置，构建一个目标镜像。
5. 创建一个容器，挂载模型及持久化目录，通过宿主机 HTTP 串行复测，并验证 GPU 访问及重启后数据保留。
6. 更新部署说明、测试报告和中文 COMMITLOG，按独立功能提交并推送 Git 分支。

## 已确认的环境

- 项目逻辑路径：`/home/huyanwei/projects/rail-system/code/ai-toolkit`。
- 实际测试 GPU：RTX 3090 24GB；目标兼容 NVIDIA RTX 4090，不能把本机测试表述为 4090 实测。
- Docker Engine 29.7.2，宿主机已提供 NVIDIA CDI 配置。
- Python 依赖来自 `ai-toolkit_312`；本地 `.venv` 初始为空，必须安装后再验证。
- 已有其他 ComfyUI 服务占用显存；测试不得擅自停止该服务。
- `.gitignore` 是已有用户改动，不纳入本任务提交。

## 提交约定

每项独立改动使用英文 commit message；同时向 `COMMITLOG.md` 追加中文动机、范围、验证和注意事项。使用提交标题关联记录（避免将提交自身 hash 写入自身文件形成循环依赖）。

## 执行结果

本地串行 API、正常微型 LoRA、运行中取消、单镜像构建、容器外串行 API/训练/取消、GPU 访问及重启持久化验证均已完成。详细证据、实测硬件和未覆盖范围见 `SERVICE_TEST_REPORT.md`。原 `.gitignore` 用户改动保持未提交。

2026-09-18 补充完成：新 .venv 的本地 API/真实训练/取消回归；扩展本地和容器串行检查至 47 项；通过公开 Python 基础镜像（镜像站及构建代理）从零安装依赖并部署，同一容器再次完成 API/训练/取消/重启持久化验证。4090 实测和完整生产模型验收仍需硬件及模型配置；当前业务 VAE 跨目录软链接在容器内不可读，见部署说明。
